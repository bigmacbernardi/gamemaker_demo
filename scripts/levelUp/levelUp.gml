// Script assets have changed for v2.3.0 see
// https://help.yoyogames.com/hc/en-us/articles/360005277377 for more information
function levelUp(character){
	//update stats
	
	//check if perk was unlocked -- this should also be done when using dust!
		//apply perk if unlocked
	
	//add dust to player's character stats
}

function dustUp(character,stat,amt){
	//var stat_step = ceil((LV99[stat] - LV1[stat])/98);
	//global.party[character][stat]+=stat_step;
	//global.party[character][dust]--;
	
	//check if perk was unlocked
		//apply perk if unlocked
}