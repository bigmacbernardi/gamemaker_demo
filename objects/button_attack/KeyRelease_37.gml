/// @description Overridden bc attack always has West option and is default (at least for now)

/*if (isSelected && frameHasPassed){
	  isSelected = false;
	  show_debug_message("Trying to go left from attack "+string(id));
      leftWest.isSelected = true;
}
else if (battle_manager.allowInput && !button_item.isSelected && !button_skill.isSelected && !button_dual_wait.isSelected) isSelected = true;*/