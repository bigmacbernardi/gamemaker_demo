/// @description Passing the "isSelected" status to the next button

//template that all buttons can share
/* if (isSelected) {
      left_button.isSelected = true;
	  isSelected = false;  
   }
   
*/
//function that only goes on the "Default" button
/* if (!(button_attack.isSelected || left_button.isSelected ||  right_button.isSelected || etc...) {
      isSelected = true;
   }
   
*/
   